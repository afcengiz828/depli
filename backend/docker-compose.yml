services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: depli
      POSTGRES_PASSWORD: depli_dev_pass
      POSTGRES_DB: depli_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
